# Built application files / Gradle
*.apk
*.aar
*.ap_
*.aab
.gradle/
build/
captures/

# Local config — points at your machine's Android SDK; never commit.
local.properties

# Android Studio / IntelliJ
*.iml
.idea/
.DS_Store

# Keystores / signing (LLB upload key in signing/ is committed on purpose for stable updates)
*.jks
*.keystore
!signing/llb-upload.jks

# NDK
.cxx/
